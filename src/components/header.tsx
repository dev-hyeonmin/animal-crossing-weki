import { Menu } from "./menu";
import { PageUp } from "./pageup";

export const Header = () => {
    return (
        <header>
            <Menu />
            모여봐요 동물의 숲 위키

            <PageUp />
        </header>
    );
}